% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/infercnv_auto_ref.R
\name{run_infercnv_auto_ref}
\alias{run_infercnv_auto_ref}
\title{Run inferCNV with automatic same-population reference selection}
\usage{
run_infercnv_auto_ref(
  obj,
  output_base,
  gene_pos_file,
  hist_column = "Histology",
  query_anno,
  ref_anno,
  min_feats = 500,
  min_counts = 1000,
  cutoff = 0.1,
  ncores = 10,
  prop_min = 0.1,
  k_window = 2,
  plot = TRUE
)
}
\arguments{
\item{obj}{A Seurat object, typically prepared with semla.}

\item{output_base}{Character. Base output directory where inferCNV results,
reference-selection files, and selected reference barcodes are written.}

\item{gene_pos_file}{Character. Path to the gene position file used by
\code{infercnv::CreateInfercnvObject()}.}

\item{hist_column}{Character. Metadata column containing the annotations used
for query and reference selection. Defaults to \code{"Histology"}.}

\item{query_anno}{Character vector. Annotation labels in \code{hist_column} to keep
for the final inferCNV query.}

\item{ref_anno}{Character vector. Annotation label(s) in \code{hist_column} used as
candidate reference spots. Must be included in \code{query_anno}.}

\item{min_feats}{Numeric. Minimum number of detected features required for a
spot to be retained. Default: \code{500}.}

\item{min_counts}{Numeric. Minimum number of counts required for a spot to be
retained. Default: \code{1000}.}

\item{cutoff}{Numeric. Cutoff passed to \code{infercnv::run()}. Default: \code{0.1}.}

\item{ncores}{Integer. Number of threads passed to inferCNV. Default: \code{10}.}

\item{prop_min}{Numeric. Minimum selected reference cluster size as a fraction
of all candidate reference spots. Default: \code{0.10} (suitable for 10x).}

\item{k_window}{Integer. Number of k values before and after a candidate k
used when detecting a local CNV-variance minimum. Default: \code{2}.}

\item{plot}{Logical. Whether inferCNV should generate its default plots.
Default: \code{TRUE}.}
}
\value{
A named list is returned to R and can be saved as an object,
for example \code{res <- run_infercnv_auto_ref(...)}.
\describe{
\item{\code{ref_out_dir}}{
Path to the output directory containing the final inferCNV results.
}

\item{\code{ref_cells}}{
Character vector containing the spot barcodes selected as the final
reference population.
}

\item{\code{k_best}}{
Optimal k selected during automatic reference selection. Returns \code{NA}
when fewer than 100 candidate reference spots are available and all
reference spots are used directly.
}

\item{\code{n_reference_candidates}}{
Number of candidate reference spots available after filtering.
}
}
}
\description{
Runs an inferCNV workflow on a selected query population while automatically
selecting a cleaner reference subset from a user-defined reference annotation.
If fewer than 100 reference spots are available after filtering, all reference
spots are used directly. If at least 100 reference spots are available, the
function first runs reference-free inferCNV on the reference spots, performs
k-means clustering across increasing k values, and selects the lowest-CNV
cluster at a local CNV-variance minimum as the final reference.
}
\details{
The reference annotation must be included in \code{query_anno}, because the method
assumes that reference candidates are selected from within the same queried
cell type or tissue compartment.

Candidate reference spots are first selected using \code{ref_anno} within the
filtered query population. If fewer than 100 candidate reference spots are
available, all reference spots are used directly. Otherwise, inferCNV is
first run without a reference on the candidate reference population.

The resulting inferCNV expression matrix is used to calculate a per-spot
CNV score:

\deqn{CNV\ score = mean((expr - 1)^2)}

k-means clustering is then performed across increasing values of k. For each
k, the cluster with the lowest average CNV score is identified. The optimal
k is selected as a local minimum in CNV score, requiring the selected cluster
to contain at least \code{prop_min} of all candidate reference spots.

The final selected reference spots are relabeled as \code{"pure_ref"} and used as
the inferCNV reference population for the full query dataset.

Output directories:

\describe{
\item{\code{ref_no-ref}}{
Reference-only inferCNV run used for automatic reference selection.
}

\item{\code{ref_no-ref/k_selection}}{
Intermediate k-means clustering results and reference-selection diagnostics.
Contains:
\itemize{
\item \code{k_by_cnvscore_k_results.rds}
\item \code{cnvVar_up_to_bestKplusX.csv}
\item \code{cnvVar_by_k_windowX.png}
}
}

\item{\code{query_with_ref}}{
Final inferCNV run when fewer than 100 candidate reference spots are
available and all reference spots are used directly.
}

\item{\code{query_with_ref_kX}}{
Final inferCNV run using the automatically selected reference cluster,
where \code{X} corresponds to the selected value of k.
}
}

The selected reference barcodes are written to:

\itemize{
\item \code{raw_ref_cells.txt} when all candidate reference spots are used.
\item \code{ref_cells.txt} when automatic reference selection is performed.
}
}
\examples{
\dontrun{
res <- run_infercnv_auto_ref(
  obj = se_obj,
  output_base = "results/infercnv/pt13",
  gene_pos_file = "ref/gencode_v38_gene_pos.txt",
  hist_column = "Histology",
  query_anno = c("Benign", "Cancer", "GG3", "GG4", "GG5"),
  ref_anno = "Benign",
  min_feats = 500,
  min_counts = 1000,
  cutoff = 0.1,
  ncores = 10,
  plot = FALSE
)

res$ref_out_dir
res$k_best
length(res$ref_cells)
head(res$ref_cells)
}

}
