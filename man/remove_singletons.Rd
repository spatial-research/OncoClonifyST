% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tme_functions.R
\name{remove_singletons}
\alias{remove_singletons}
\title{Remove spatial singleton labels}
\usage{
remove_singletons(
  object,
  label_col,
  sample_col = "sample_id",
  nNeighbors = 6,
  maxDist_method = c("minimum", "median"),
  new_col = NULL,
  section_map = NULL
)
}
\arguments{
\item{object}{Seurat object.}

\item{label_col}{Metadata column containing labels to clean.}

\item{sample_col}{Metadata column identifying tissue sections or samples.
Defaults to \code{"sample_id"}.}

\item{nNeighbors}{Number of spatial neighbors to use when constructing the
spatial neighbor graph. Passed to \code{GetSpatialNetwork()}.}

\item{maxDist_method}{Character string specifying how to compute the
distance cut‐off.  Either "minimum" (use semla’s default based on
the minimum nearest neighbour distance) or "median" (compute a
cut‐off from the median nearest neighbour distance).  Defaults to
"minimum".}

\item{new_col}{Name of the metadata column where cleaned labels will be
stored. If \code{NULL}, defaults to \code{paste0(label_col, "_nosingletons")}.}

\item{section_map}{Named character vector mapping sample IDs to spatial
network section IDs, for example \code{c("A1" = "-1", "A2" = "-2")}.}
}
\value{
Seurat object with cleaned labels and same-label neighbor counts.
}
\description{
Removes labeled spots that do not have any neighboring spot with the same
label within each tissue section. A spot is considered a singleton if it has
a non-missing label but zero same-label neighbors in the spatial neighbor
graph.
}
