% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/infercnv_region_visualization.R
\name{plot_infercnv_chr_boxplots}
\alias{plot_infercnv_chr_boxplots}
\title{Plot inferCNV signal across a chromosome or chromosome arm}
\usage{
plot_infercnv_chr_boxplots(
  infercnv_obj,
  chromosome,
  split_by_arm = FALSE,
  centromere_pos = NULL,
  clones_to_plot = NULL,
  plot_title = NULL,
  x_lab = NULL,
  y_lab = NULL,
  line_width = 1.2,
  facet_ncol = 1,
  free_y = TRUE,
  box_fill = "lightblue"
)
}
\arguments{
\item{infercnv_obj}{An inferCNV object containing expression data, gene ordering, and observation groups.}

\item{chromosome}{Character string naming the chromosome to plot. Example \code{"chr8"}.}

\item{split_by_arm}{Logical. If \code{TRUE}, draws a vertical line at the centromere position to separate chromosome arms.}

\item{centromere_pos}{Optional named vector or list of centromere positions, where names correspond to chromosomes.
Required if \code{split_by_arm = TRUE}.}

\item{clones_to_plot}{Optional character vector of clone names to include. If \code{NULL}, all available clones and references are shown.}

\item{plot_title}{Optional custom plot title.}

\item{x_lab}{Optional x-axis label.}

\item{y_lab}{Optional y-axis label.}

\item{line_width}{Numeric line width for the chromosome arm separator.}

\item{facet_ncol}{Number of columns to use when faceting by clone.}

\item{free_y}{Logical. If \code{TRUE}, each facet uses its own y-axis scale.}

\item{box_fill}{Fill color for the boxplots.}
}
\value{
A \code{ggplot2} object showing gene-level inferCNV signal across the selected chromosome for each reference or clone group.
}
\description{
This function visualizes inferCNV expression values for genes along a selected chromosome,
grouped by reference and clone labels, and optionally marks a centromere-based split between chromosome arms.
}
\details{
\itemize{
\item Genes are ordered by genomic start position using \code{infercnv_obj@gene_order}.
\item Only genes present in both \code{infercnv_obj@gene_order} and \code{infercnv_obj@expr.data} are plotted.
\item Reference spots are labeled as \code{"Reference"} and observed spots are grouped using inferCNV clone assignments.
\item If \code{split_by_arm = TRUE}, a dashed line is added at the supplied centromere position.
}
}
\examples{
\dontrun{
plot_infercnv_chr_boxplots(
  infercnv_obj = infercnv_final,
  chromosome = "chr8",
  split_by_arm = TRUE,
  centromere_pos = c(chr8 = 45600000),
  clones_to_plot = c("Clone.1", "Clone.2"),
  box_fill = "lightblue"
)
}

}
