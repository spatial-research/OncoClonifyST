% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tme_functions.R
\name{remove_small_spatial_islands}
\alias{remove_small_spatial_islands}
\title{Remove small spatial islands of labels}
\usage{
remove_small_spatial_islands(
  object,
  label_col = "clone_na0",
  sample_col = "sample_id",
  nNeighbors = 6,
  min_island_size = 2,
  maxDist_method = c("minimum", "median"),
  new_col = NULL,
  section_map = NULL
)
}
\arguments{
\item{object}{Seurat object.}

\item{label_col}{Metadata column containing labels to clean.
Defaults to \code{"clone_na0"}.}

\item{sample_col}{Metadata column identifying tissue sections or samples.
Defaults to \code{"sample_id"}.}

\item{nNeighbors}{Number of spatial neighbors to use when constructing the
spatial neighbor graph. Passed to \code{GetSpatialNetwork()}.}

\item{min_island_size}{Minimum connected island size to retain.}

\item{maxDist_method}{Character string specifying how to compute the
distance cut‐off.  Either "minimum" or "median".  Defaults to
"minimum".}

\item{new_col}{Name of the metadata column where cleaned labels will be
stored. If \code{NULL}, defaults to \code{paste0(label_col, "_no_small_islands")}.}

\item{section_map}{Named character vector mapping sample IDs to spatial
network section IDs, for example \code{c("A1" = "-1", "A2" = "-2")}.}
}
\value{
Seurat object with cleaned labels and island sizes.
}
\description{
Removes connected components of same-label spots that are smaller than a
specified minimum island size within each tissue section.
}
